get:
  tags:
    - admin
  summary: Retrieve additional information
  responses:
    "200":
      description: Successful response with additional information
      content:
        application/json:
          schema:
            type: object
            properties:
              info:
                type: string
                example: "This is the additional information."

